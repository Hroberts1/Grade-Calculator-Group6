#include "assignment_data.h"
#include "assignment_data.h"

using namespace std;
// Constructor definition
AssignmentData::AssignmentData(const string& name, const string& type, double grade)
    : assignmentName(name), assignmentType(type), grade(grade) {}

// Destructor definition
AssignmentData::~AssignmentData() {}
